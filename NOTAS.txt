docker compose up -d --build

docker restart openecoe-all-api-

# Limpiar Chrono
docker compose stop  openecoe-all-webui-1
# reset absoluto del cronómetro.
docker exec -it openecoe-all-redis-1 redis-cli flushall
# consulta el token:
http://localhost:5001/backend/api/v1/ecoes/1/load
# upodate en DB
UPDATE ecoe SET 
  chrono_token = 'sssss', -- Pega aquí el churro de texto completo
  status = 'PUBLISHED' 
WHERE id = 1;

## built de un contenedor (por nombre del servicio en compose)
docker compose up -d --build webui

# modificaciones:
modificamos:
chrono.components.ts
#redis
# ver las keys
docker exec -it openecoe-all-redis-1 redis-cli keys "*"

# Acceso bash
docker exec -it openecoe-all-webui-1 /bin/sh
docker exec -it openecoe-all-db-1 /bin/sh
docker exec -it openecoe-all-api-1 /bin/sh

# ver la fecha del contenedor de mysql:
docker exec -it openecoe-all-db-1 mysql -u openecoe -p -e "SELECT @@global.time_zone, @@session.time_zone, NOW();"
buscamos y reemplazamos utcnow, por now

model\shift.py
api\shift.py
model\ECOE.py

# Borra los estados guardados con la hora errónea
docker exec -it openecoe-all-api-1 rm -f /tmp/config_ecoe_*.json /tmp/round.*.status /tmp/chrono.*.status
Simpre hacer un down y build porque se cachea  un montón

== Script Javier:
nohup python3 create_ecoe_test.py > salida_ecoe.log 2>&1 &